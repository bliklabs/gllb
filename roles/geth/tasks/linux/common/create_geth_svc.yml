---

- name: Copy geth.service template
  template:
    src: geth.service.j2
    dest: /lib/systemd/system/geth.service

- name: Reload daemon
  become: true
  systemd:
    daemon_reload: true

- name: Enable geth.service
  become: yes
  systemd:
    name: geth
    enabled: yes
    state: started

...
