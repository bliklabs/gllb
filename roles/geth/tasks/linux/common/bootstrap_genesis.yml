---

#- name: Run remote gen wallets
#  command: "/usr/bin/env python3 {{ root_dir }}/tooling/bootstrap_wallet_sync.py {{ genesis_options['num_wallet'] }} {{ genesis_options['num_signers'] }} {{ genesis_options['root_dir'] }} {{ genesis_options['sync_dir'] }}"
#  become: yes
#  register: gen_wallets_output

#- name: Set gen_wallets fact
#  set_fact:
#    gen_wallets: "{{ gen_wallets_output.stdout | from_json }}"

#- name: Debug gen_wallets
#  debug:
#    var: gen_wallets

...
